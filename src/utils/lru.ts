export class LRU<K,V>{ private limit:number; private map = new Map<K,V>(); constructor(limit=20){ this.limit = limit } get(k:K){ const v=this.map.get(k); if(v){ this.map.delete(k); this.map.set(k,v) } return v } set(k:K,v:V){ if(this.map.has(k)) this.map.delete(k); this.map.set(k,v); if(this.map.size>this.limit){ const fk=this.map.keys().next().value as K; this.map.delete(fk) } } }